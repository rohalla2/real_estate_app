<h1>Listing payments</h1>


<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Date/Time</th>
      <th>Tenant Name</th>
      <th>Property Owner</th>
      <th>Amount</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @payments.each do |payment| %>
      <tr class="clickableRow" href= "<%= payment_path(payment) %>">
        <!-- finds the manager of the property -->
        <% property = Property.find_by(id: payment.property_id).user_id %>
        <% manager = User.find_by(id: property ) %>

        <!-- Only display if user received payment or sent payment -->
        <% if manager.id == @user.id || payment.user_id == @user.id %>
            <td><%= payment.updated_at %></td>
            <td><%= payment.user.first_name + " " + payment.user.last_name%></td>
            <td><%= manager.first_name + " " + manager.last_name %></td>
            <td><%= payment.amount %></td>
            <td><%= link_to 'Show', payment %></td>
            <td><%= link_to 'Edit', edit_payment_path(payment) %></td>
            <td><%= link_to 'Destroy', payment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
